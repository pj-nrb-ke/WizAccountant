# Candidate intent tests (auto-exported)

Generated by `scripts/export-insight-triage.ps1` or `scripts/weekly-pilot-review.ps1`.

**Do not commit blindly.** Review each case, then move approved entries to `tests/intents/*.json` and `DOCS/Real_Insight_Queries.md`.

See `DOCS/Pilot_Stabilization_Workflow.md`.
