import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WizAccounts — AI-Powered Finance Intelligence",
  description:
    "Executive-grade finance intelligence for Sage 200 Evolution: Insight AI, reconciliation, approvals, and mobile — built on secure on-prem WizConnector.",
  openGraph: {
    title: "WizAccounts — AI-Powered Finance Intelligence",
    description:
      "Live Sage data, intent-first AI, maker-checker approvals, and CFO dashboards — without legacy ERP clutter.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full scroll-smooth`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col antialiased">
        <ThemeProvider>{children}</ThemeProvider>
      </body>
    </html>
  );
}
